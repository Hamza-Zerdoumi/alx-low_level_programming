#include "main.h"
/**
*_isdigit - functions to check if the character is upper or not.
*
*@c: character
*
*Return: 1 if is digit and 0 otherwise
*/

int _isdigit(int c)
{
	if (c >=  48 &&  c < 58)
		return (1);
	else
		return (0);
}







